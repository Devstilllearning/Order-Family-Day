name: CI/CD Build & Deploy

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-validate:
    name: Build & Validate Application
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Run Lint & Type Check
        run: npm run lint

      - name: Build Application
        run: npm run build
        env:
          NODE_ENV: production

  # Optional Production Deployment Job Template
  # Uncomment and configure this job once you are ready to deploy!
  # deploy-production:
  #   name: Deploy to Production
  #   needs: build-and-validate
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
  #   
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v4
  #
  #     # OPTION A: Deploy to Google Cloud Run (Matches Sandbox Container Runtime)
  #     # 1. Set up GCP Secrets (GCP_SA_KEY, GCP_PROJECT_ID) in your GitHub repository secrets.
  #     # 2. Uncomment the steps below to automate container build & serverless deploy.
  #     #
  #     # - name: Google Auth
  #     #   uses: google-github-actions/auth@v2
  #     #   with:
  #     #     credentials_json: ${{ secrets.GCP_SA_KEY }}
  #     #
  #     # - name: Set up Cloud SDK
  #     #   uses: google-github-actions/setup-gcloud@v2
  #     #
  #     # - name: Authorize Docker Push
  #     #   run: gcloud auth configure-docker
  #     #
  #     # - name: Build and Push Container Image
  #     #   run: |
  #     #     docker build -t gcr.io/${{ secrets.GCP_PROJECT_ID }}/warung-hangat:${{ github.sha }} .
  #     #     docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/warung-hangat:${{ github.sha }}
  #     #
  #     # - name: Deploy to Cloud Run
  #     #   uses: google-github-actions/deploy-cloudrun@v2
  #     #   with:
  #     #     service: warung-hangat
  #     #     image: gcr.io/${{ secrets.GCP_PROJECT_ID }}/warung-hangat:${{ github.sha }}
  #     #     region: asia-southeast1
  #     #     flags: '--allow-unauthenticated --port=3000'
  #
  #     # OPTION B: Deploy via Render Webhook
  #     # - name: Trigger Render Deploy Hook
  #     #   run: |
  #     #     curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
